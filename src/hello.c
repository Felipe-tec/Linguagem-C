 // Importação da biblioteca padrão pra o uso de teclado e monotor
 #include <stdio.h>

 int main(){
 // para exibir uma mensagem na tela iremos usar
 // o comando printf(exibir formato)
 printf("hello, word!\n");
 // informar a o compilador que o programa
 // acabou
  return 0;
}